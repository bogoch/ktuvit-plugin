// Use an integer for version numbers
version = 3

cloudstream {
    description = "כתוביות בעברית מ-Ktuvit"
    authors = listOf("yonatan")

    /**
     * Status int as one of the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta-only
     **/
    status = 3

    tvTypes = listOf("Movie", "TvSeries")
    iconUrl = "https://www.ktuvit.me/favicon.ico"
}
